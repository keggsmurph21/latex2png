version: 2
jobs:
    build:
        macos:
            xcode: "10.0.0"
        steps:
            - checkout
            - run: ./scripts/install
            - run: which convert
            - run: which pdflatex
            - run: sudo pip install nox
            - run: nox
